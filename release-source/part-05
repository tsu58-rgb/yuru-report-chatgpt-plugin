quire_once ABSPATH . 'wp-admin/includes/media.php'; require_once ABSPATH . 'wp-admin/includes/image.php'; $tmp_original = download_url( $url, 20 ); if ( is_wp_error( $tmp_original ) ) { return $tmp_original; } $size = filesize( $tmp_original ); if ( false === $size || $size > self::MAX_BYTES ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_image_too_large', '画像サイズが10MBを超えています。' ); } $actual_mime = wp_get_image_mime( $tmp_original ); $allowed = array( 'image/jpeg', 'image/png', 'image/webp' ); if ( ! in_array( $actual_mime, $allowed, true ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_bad_image_type', '対応していない画像形式です: ' . ( $actual_mime ?: '不明' ) ); } $post_slug = sanitize_title( get_post_field( 'post_name', $post_id ) ); if ( '' === $post_slug ) { $post_slug = 'walking-report-' . absint( $post_id ); } $sequence = str_pad( (string) ( absint( $index ) + 1 ), 2, '0', STR_PAD_LEFT ); $filename = sanitize_file_name( $post_slug . '-' . $sequence . '.webp' ); $upload_path = $tmp_original; if ( 'image/webp' === $actual_mime ) { $dimensions = @getimagesize( $tmp_original ); if ( is_array( $dimensions ) && ( $dimensions[0] > self::MAX_IMAGE_DIMENSION || $dimensions[1] > self::MAX_IMAGE_DIMENSION ) ) { if ( ! wp_image_editor_supports( array( 'mime_type' => 'image/webp' ) ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_webp_resize_unsupported', 'WebP画像が長辺1920pxを超えていますが、サーバー側でWebPを縮小できません。GPT側で長辺1920px以下のWebPにしてから再送してください。' ); } $editor = wp_get_image_editor( $tmp_original ); if ( is_wp_error( $editor ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_image_editor_error', 'WebP画像を開けませんでした: ' . $editor->get_error_message() ); } $resize_result = $editor->resize( self::MAX_IMAGE_DIMENSION, self::MAX_IMAGE_DIMENSION, false ); if ( is_wp_error( $resize_result ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_image_resize_error', 'WebP画像の縮小に失敗しました: ' . $resize_result->get_error_message() ); } $editor->set_quality( self::WEBP_QUALITY ); $tmp_dir = get_temp_dir(); $tmp_resized = trailingslashit( $tmp_dir ) . wp_unique_filename( $tmp_dir, $filename ); $save_result = $editor->save( $tmp_resized, 'image/webp' ); @unlink( $tmp_original ); if ( is_wp_error( $save_result ) || empty( $save_result['path'] ) || ! file_exists( $save_result['path'] ) ) { @unlink( $tmp_resized ); $message = is_wp_error( $save_result ) ? $save_result->get_error_message() : '変換後ファイルがありません。'; return new WP_Error( 'yrs_webp_resize_error', 'WebP画像の縮小保存に失敗しました: ' . $message ); } if ( 'image/webp' !== wp_get_image_mime( $save_result['path'] ) ) { @unlink( $save_result['path'] ); return new WP_Error( 'yrs_not_real_webp', '縮小後の画像が実際のWebP形式ではありませんでした。' ); } $upload_path = $save_result['path']; } } else { if ( ! wp_image_editor_supports( array( 'mime_type' => 'image/webp' ) ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_webp_conversion_unsupported', 'このサーバーのWordPress画像処理ではJPEG/PNGをWebPへ変換できません。GPT側でWebPへ変換した画像を送信してください。' ); } $editor = wp_get_image_editor( $tmp_original ); if ( is_wp_error( $editor ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_image_editor_error', '画像変換を開始できませんでした: ' . $editor->get_error_message() ); } $dimensions = $editor->get_size(); if ( is_array( $dimensions ) && ( $dimensions['width'] > self::MAX_IMAGE_DIMENSION || $dimensions['height'] > self::MAX_IMAGE_DIMENSION ) ) { $resize_result = $editor->resize( self::MAX_IMAGE_DIMENSION, self::MAX_IMAGE_DIMENSION, false ); if ( is_wp_error( $resize_result ) ) { @unlink( $tmp_original ); return new WP_Error( 'yrs_image_resize_error', '画像の縮小に失敗しました: ' . $resize_result->get_error_message() ); } } $editor->set_quality( self::WEBP_QUALITY ); $tmp_dir = get_temp_dir(); $tmp_webp = trailingslashit( $tmp_dir ) . wp_unique_filename( $tmp_dir, $filename ); $save_result = $editor->save( $tmp_webp, 'image/webp' ); @unlink( $tmp_original ); if ( is_wp_error( $save_result ) || empty( $save_result['path'] ) || ! file_exists( $save_result['path'] ) ) { @unlink( $tmp_webp ); $message = is_wp_error( $save_result ) ? $save_result->get_error_message() : '変換後ファイルがありません。'; return new WP_Error( 'yrs_webp_conversion_error', 'WebPへの変換に失敗しました: ' . $message ); } if ( 'image/webp' !== wp_get_image_mime( $save_result['path'] ) ) { @unlink( $save_result['path'] ); return new WP_Error( 'yrs_not_real_webp', 'サーバーがWebP変換要求をJPEG/PNGとして保存したため、画像の登録を中止しました。GPT側でWebPへ変換した画像を送信してください。' ); 